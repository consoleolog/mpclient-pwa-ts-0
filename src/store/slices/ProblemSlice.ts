import {createSlice} from "@reduxjs/toolkit";

const ProblemSlice = createSlice({
    name: "ProblemSlice",
    initialState: {

    },
    reducers : {
        changeProblemSlice(state){

        }
    }
})

export {}